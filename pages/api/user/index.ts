import database from "@/utils/server/mongodb";

// const options: any = {
//   useUnifiedTopology: true,
//   useNewUrlParser: true,
// };

// const client = new MongoClient(process.env.MONGODB_URI, options);

// client.connect((err) => {
//   const collection = client.db("test").createUser("hi");
//   // perform actions on the collection object
//   client.close();
// });

// async function user() {
//   client.connect();
//   //   const user = db.runCommand({ connectionStatus: 1 }).authInfo
//   //     .authenticatedUsers[0];
//   //   return user;
// }

export default null;
