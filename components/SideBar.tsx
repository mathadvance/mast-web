function SideRow({ link, label, icon }) {
    return (
        <div className="">

        </div>
    );
};

function SideBar() {
    return (
        <div>

        </div>
    );
};

export default SideBar;